# Images Directory

This directory manages the container images built for applications running in the cluster (e.g., eliza-runtime, mem0-api-server). It includes the necessary Dockerfiles and resources required to construct these custom images.