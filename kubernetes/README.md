# Kubernetes Directory

This directory contains the manifest files for FluxCD to manage the applications in the Kubernetes cluster. It organizes the cluster configurations, including core services, applications, network resources, and Flux synchronization settings.