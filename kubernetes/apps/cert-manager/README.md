# Cert-Manager Namespace

This directory manages the applications and configurations for the `cert-manager` namespace within the Kubernetes cluster.

## Applications

- **cert-manager**: Manages TLS certificates for services in the cluster.
