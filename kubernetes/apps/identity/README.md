# Identity Namespace

This directory manages the applications and configurations for the `identity` namespace within the Kubernetes cluster.

## Applications

- **authentik**: An open-source identity provider and SSO application.
- **vaultwarden**: An alternative implementation of the Bitwarden server API.