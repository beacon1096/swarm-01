# Registry Namespace

This directory manages the applications and configurations for the `registry` namespace within the Kubernetes cluster.

## Applications

- **talos-image-factory**: Self-hosted Talos Image Factory with custom extension catalog support.
- **zot**: OCI-native container image registry.
