# Flux-System Namespace

This directory manages the core gitops applications and configurations for the `flux-system` namespace within the Kubernetes cluster.

## Applications

- **flux-instance**: Manages the instances of the Flux components.
- **flux-operator**: The operator for Flux.