---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-logs
spec:
  chartRef:
    kind: OCIRepository
    name: victoria-logs-single
  interval: 1h
  values:
    server:
      retentionPeriod: 30d
      # Pin to virt-01 so the RWO PVC stays on a fixed Harvester host
      nodeSelector:
        kubernetes.io/hostname: virt-01
      persistentVolume:
        enabled: true
        storageClassName: harvester
        size: 20Gi
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          memory: 512Mi
