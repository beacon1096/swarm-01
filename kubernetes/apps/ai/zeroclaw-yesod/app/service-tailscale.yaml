---
apiVersion: v1
kind: Service
metadata:
  name: zeroclaw-yesod-tailscale
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "zeroclaw-yesod"
spec:
  type: ClusterIP
  selector:
    app: zeroclaw-yesod
  ports:
    - name: http
      port: 42617
      targetPort: 42617
      protocol: TCP
