---
apiVersion: v1
kind: Service
metadata:
  name: memoh-web-tailscale
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: memoh-ii
spec:
  selector:
    app: memoh-web
  ports:
    - port: 8082
      targetPort: 8082
      name: http
