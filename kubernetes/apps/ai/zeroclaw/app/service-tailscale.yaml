---
apiVersion: v1
kind: Service
metadata:
  name: zeroclaw-tailscale
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "zeroclaw"
spec:
  type: ClusterIP
  selector:
    app: zeroclaw
  ports:
    - name: http
      port: 42617
      targetPort: 42617
      protocol: TCP