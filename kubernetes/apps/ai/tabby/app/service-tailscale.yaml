---
apiVersion: v1
kind: Service
metadata:
  name: tabby-tailscale
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "tabby"
spec:
  type: ClusterIP
  selector:
    app: tabby
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
