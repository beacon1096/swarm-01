# AI Namespace

This directory manages the applications and configurations for the `ai` namespace within the Kubernetes cluster.

## Applications

- **eliza**: Configuration for the eliza-runtime application.
- **mem0**: Configuration for the mem0-api-server and related services.
