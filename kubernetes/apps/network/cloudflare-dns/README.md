# Cloudflare DNS

ExternalDNS provider for automatic public DNS record management.

- **Namespace:** network
- **Clusters:** Talos I, Talos II
- **Chart:** cloudflare-dns (OCI)
- **Config:** DNSEndpoint CRD source, gateway-httproute sync, txt prefix "k8s."
