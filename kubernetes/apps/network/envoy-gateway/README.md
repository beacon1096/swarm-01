# Envoy Gateway

Gateway API implementation (Envoy-based) for ingress routing.

- **Namespace:** network
- **Clusters:** Talos I, Talos II
- **Chart:** envoy-gateway (OCI)
- **Config:** Kubernetes provider, GatewayNamespace deployment
- **Note:** Talos II skips CRD install (Gateway API pre-installed)
