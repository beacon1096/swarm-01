# Default Namespace

This directory manages the applications and configurations for the `default` namespace within the Kubernetes cluster.

## Applications

- **echo**: A simple echo server used for testing and validating ingress and routing.