# Bootstrap Directory

This directory handles the initial cluster setup via Helmfile. It's responsible for the foundational deployments into the cluster necessary before FluxCD can take over operations, such as core Custom Resource Definitions (CRDs) and initial application resources.